/**
 * Конструктор для нашего приложения
 * @param selector Селектор дом элемента, куда будет встраиваться приложение
 * @param slides Массив имен файлов слайдов-картинок
 **/
function App(selector, slides) {
  if (!$.isArray(slides)) {
    throw 'Incorrect slide array format'
  } 
}
